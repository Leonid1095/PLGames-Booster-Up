# PLGames Booster UP - Nginx Configuration
# Ports offset (15xxx) to avoid conflicts with dns_* project on this server

upstream plgames_api {
    server 127.0.0.1:18000;
}

upstream plgames_web {
    server 127.0.0.1:13000;
}

# Will be activated after domain purchase
# For now, API is accessible directly on :18000
# server {
#     listen 80;
#     server_name plgames.example.com;
#
#     location /api/ {
#         proxy_pass http://plgames_api;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
#
#     location / {
#         proxy_pass http://plgames_web;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
